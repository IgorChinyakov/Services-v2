# FuncActionSandbox

Small playground for practicing `Func`, `Action`, `Task`, `async/await`, and scoped helper patterns.

Run:

```bash
dotnet run --project sandbox/FuncActionSandbox/FuncActionSandbox.csproj
```

Work through TODOs in `Exercises.cs`.
